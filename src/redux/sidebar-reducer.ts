import {ActionTypes, ProfilePageType, SidebarType} from "./store";

let initialState: SidebarType = {}

export const sidebarReducer = (state = initialState, action: ActionTypes) => {
   return state
}